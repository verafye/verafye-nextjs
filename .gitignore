node_modules/
.next/
out/
.DS_Store
*.log
.env
.env.local
*.bak*
out.zip

# Build artifacts and local env (added in QA hygiene pass)
.env*.local
*.zip
!public/downloads/resources.zip
.env.production
sales-assets/

# sales-assets/ holds INTERNAL sales collateral (not part of the website build).
# Already isolated: not imported, not in public/, not in out/. Untrack with
# `git rm --cached -r sales-assets` if a clean public handoff is needed.

# Temporary filesystem artifacts
.fuse_hidden*
