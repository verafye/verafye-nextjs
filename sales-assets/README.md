# Sales Assets (INTERNAL - not part of the website build)

Files in this folder are internal sales collateral. They live in the repo root,
OUTSIDE the Next.js app (`VerafyeV3.1/`), so they are never compiled, routed, or
exported to the public site (`output: 'export'` only emits `VerafyeV3.1/app` routes
and `VerafyeV3.1/public`).

## verafye-risk-shadowing-sample-evidence-pack-INTERNAL.html
Self-contained, synthetic-data sample of a Risk Shadowing Review evidence pack.
Open in any browser; use the browser's Print -> Save as PDF to produce a PDF for
sharing during qualified walkthroughs. Synthetic data only. Not for public distribution.
